## Testing Requirements (Mandatory)

After completing ANY code implementation, you MUST:

### 1. Unit Tests
- Write unit tests for all new React components
- Write unit tests for utility functions
- Cover: rendering, user interactions, edge cases, error states
- **Location: colocated.** `foo.ts` sits next to `foo.test.ts`, in the same directory. There is no `tests/` tree — do not create one.

### 2. Test Execution
- Run all new tests before marking work complete
- Fix any failing tests before proceeding
- Report test counts and pass/fail status

### Troubleshooting
- NEVER MAKE ASSUMPTIONS - If there are checks in the database or needed to be done by me, I will do them.  When you make assumptions we go down rabbit holes and it takes longer to fix things.
- MAKE ALL FIXES AND CODE CHANGES AS SIMPLE AS HUMANLY POSSIBLE. THEY SHOULD ONLY IMPACT NECESSARY CODE RELEVANT TO THE TASK AND NOTHING ELSE. IT SHOULD IMPACT AS LITTLE CODE AS POSSIBLE. YOUR GOAL IS TO NOT INTRODUCE ANY BUGS. IT'S ALL ABOUT SIMPLICITY